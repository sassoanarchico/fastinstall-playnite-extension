<UserControl x:Class="FastInstall.FastInstallSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="600">

    <StackPanel Margin="20">
        <TextBlock Text="FastInstall Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Source Archive Directory (Slow HDD) -->
        <TextBlock Text="Source Archive Directory (Slow HDD):" Margin="0,0,0,5"/>
        <TextBlock Text="This is where your archived games are stored. Games will be copied FROM this location."
                   FontSize="11" Opacity="0.7" Margin="0,0,0,5" TextWrapping="Wrap"/>
        <DockPanel Margin="0,0,0,15">
            <Button Content="Browse..." DockPanel.Dock="Right" Padding="15,5" Margin="10,0,0,0"
                    Command="{Binding BrowseSourceArchiveCommand}"/>
            <TextBox Text="{Binding Settings.SourceArchiveDirectory, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" Padding="5"/>
        </DockPanel>

        <!-- Fast Install Directory (SSD) -->
        <TextBlock Text="Fast Install Directory (SSD):" Margin="0,0,0,5"/>
        <TextBlock Text="This is where games will be installed/copied TO for fast access. Choose a folder on your SSD."
                   FontSize="11" Opacity="0.7" Margin="0,0,0,5" TextWrapping="Wrap"/>
        <DockPanel Margin="0,0,0,15">
            <Button Content="Browse..." DockPanel.Dock="Right" Padding="15,5" Margin="10,0,0,0"
                    Command="{Binding BrowseFastInstallCommand}"/>
            <TextBox Text="{Binding Settings.FastInstallDirectory, UpdateSourceTrigger=PropertyChanged}"
                     VerticalContentAlignment="Center" Padding="5"/>
        </DockPanel>

        <!-- Info Box -->
        <Border Background="#20808080" CornerRadius="5" Padding="15" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="How it works:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock TextWrapping="Wrap" Text="1. Place your game folders in the Source Archive Directory."/>
                <TextBlock TextWrapping="Wrap" Text="2. Each top-level subfolder will be detected as a separate game."/>
                <TextBlock TextWrapping="Wrap" Text="3. When you 'Install' a game, it will be copied to the Fast Install Directory."/>
                <TextBlock TextWrapping="Wrap" Text="4. When you 'Uninstall', the copy on the SSD is deleted (archive is preserved)."/>
            </StackPanel>
        </Border>
    </StackPanel>
</UserControl>

