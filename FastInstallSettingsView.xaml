<UserControl x:Class="FastInstall.FastInstallSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="950">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="FastInstall - Folder Configurations" 
                       FontSize="18" 
                       FontWeight="Bold"/>
        </StackPanel>

        <!-- Description -->
        <TextBlock Grid.Row="1" 
                   Text="Configure multiple source and destination folder pairs. Each enabled configuration will scan for games in the source folder and allow installation to the destination folder."
                   TextWrapping="Wrap" 
                   Margin="0,0,0,10"
                   Opacity="0.8"/>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding EditingConfigurations}"
                  SelectedItem="{Binding SelectedConfiguration, Mode=TwoWay}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  HeadersVisibility="All"
                  GridLinesVisibility="All"
                  AlternatingRowBackground="#10000000"
                  Margin="0,0,0,10">

            <DataGrid.Columns>
                <!-- Enabled Column -->
                <DataGridCheckBoxColumn Header="Enabled" 
                                        Width="70"
                                        Binding="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Source Path Column -->
                <DataGridTemplateColumn Header="Source Path (Archive HDD)" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <Button Content="..." 
                                        DockPanel.Dock="Right" 
                                        Width="30" 
                                        Margin="5,0,0,0"
                                        Command="{Binding DataContext.BrowseSourceCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <TextBox Text="{Binding SourcePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalContentAlignment="Center"
                                         Padding="5,2"/>
                            </DockPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Destination Path Column -->
                <DataGridTemplateColumn Header="Destination Path (Fast SSD)" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <DockPanel>
                                <Button Content="..." 
                                        DockPanel.Dock="Right" 
                                        Width="30" 
                                        Margin="5,0,0,0"
                                        Command="{Binding DataContext.BrowseDestinationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}"/>
                                <TextBox Text="{Binding DestinationPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         VerticalContentAlignment="Center"
                                         Padding="5,2"/>
                            </DockPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Platform Column -->
                <DataGridTemplateColumn Header="Platform" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.AvailablePlatforms, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      SelectedItem="{Binding Platform, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Emulator Column -->
                <DataGridTemplateColumn Header="Emulator (Optional)" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding DataContext.AvailableEmulators, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValuePath="EmulatorId"
                                      SelectedValue="{Binding EmulatorId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Emulator Profile Column -->
                <DataGridTemplateColumn Header="Emulator Profile" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox ItemsSource="{Binding AvailableProfilesForEmulator}"
                                      DisplayMemberPath="DisplayName"
                                      SelectedValuePath="ProfileId"
                                      SelectedValue="{Binding EmulatorProfileId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      VerticalContentAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Download Settings -->
        <StackPanel Grid.Row="3" Margin="0,15,0,10">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                <CheckBox x:Name="EnableParallelCheckBox"
                          Content="Enable Parallel Downloads"
                          IsChecked="{Binding Settings.EnableParallelDownloads, Mode=TwoWay}"
                          VerticalContentAlignment="Center"
                          Margin="0,0,15,0"
                          Checked="EnableParallelCheckBox_Checked"
                          Unchecked="EnableParallelCheckBox_Unchecked"/>
                <TextBlock Text="Max Parallel: " 
                           VerticalAlignment="Center" 
                           Margin="0,0,5,0"
                           IsEnabled="{Binding ElementName=EnableParallelCheckBox, Path=IsChecked}"/>
                <TextBox x:Name="MaxParallelTextBox"
                         Text="{Binding Settings.MaxParallelDownloads, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="50"
                         Height="22"
                         VerticalContentAlignment="Center"
                         TextAlignment="Center"
                         IsEnabled="{Binding ElementName=EnableParallelCheckBox, Path=IsChecked}"
                         PreviewTextInput="MaxParallelTextBox_PreviewTextInput"
                         LostFocus="MaxParallelTextBox_LostFocus"/>
                <TextBlock Text="(1-10)" 
                           Opacity="0.7" 
                           Margin="5,0,0,0" 
                           VerticalAlignment="Center"
                           FontSize="11"
                           IsEnabled="{Binding ElementName=EnableParallelCheckBox, Path=IsChecked}"/>
            </StackPanel>
            
            <!-- Conflict Resolution -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,10">
                <TextBlock Text="Conflict Resolution: " 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <ComboBox x:Name="ConflictResolutionComboBox"
                          SelectedItem="{Binding Settings.ConflictResolution, Mode=TwoWay}"
                          Width="200"
                          VerticalContentAlignment="Center"/>
            </StackPanel>
            
            <!-- 7-Zip Path -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="7-Zip Path: " 
                           VerticalAlignment="Center" 
                           Margin="0,0,10,0"/>
                <TextBox x:Name="SevenZipPathTextBox"
                         Text="{Binding Settings.SevenZipPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Width="300"
                         Height="22"
                         VerticalContentAlignment="Center"
                         Margin="0,0,10,0"/>
                <Button Content="Browse..."
                        Width="80"
                        Height="22"
                        Click="BrowseSevenZipButton_Click"
                        Margin="0,0,10,0"/>
                <Button Content="Download 7-Zip"
                        Width="120"
                        Height="22"
                        Click="DownloadSevenZipButton_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    Margin="0,0,0,15">
            <Button Content="Add Configuration" 
                    Padding="15,8" 
                    Margin="0,0,10,0"
                    Command="{Binding AddConfigurationCommand}"/>
            <Button Content="Remove Selected" 
                    Padding="15,8"
                    Command="{Binding RemoveConfigurationCommand}"/>
        </StackPanel>

        <!-- Info Box -->
        <Border Grid.Row="5" 
                Background="#20808080" 
                CornerRadius="5" 
                Padding="15">
            <StackPanel>
                <TextBlock Text="Configuration Guide:" FontWeight="Bold" Margin="0,0,0,8"/>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Enabled: Check to activate this configuration. Unchecked configurations will be ignored."/>
                </TextBlock>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Source Path: Folder on your slow HDD where game archives are stored."/>
                </TextBlock>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Destination Path: Folder on your fast SSD where games will be copied for installation."/>
                </TextBlock>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,5">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Platform: Type of games in this folder (affects auto-detection and play actions)."/>
                </TextBlock>
                
                <TextBlock TextWrapping="Wrap" Margin="0,0,0,10">
                    <Run Text="• " FontWeight="Bold"/>
                    <Run Text="Emulator: (Optional) Select an emulator configured in Playnite. Leave as '(None / Auto-detect)' to use auto-detection or Playnite's default emulator for the platform."/>
                </TextBlock>

                <Separator Margin="0,5,0,10"/>

                <TextBlock Text="Supported Platforms:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBlock TextWrapping="Wrap" FontSize="11" Opacity="0.9">
                    PC • PlayStation 3 (RPCS3) • PlayStation 2 (PCSX2) • PSP (PPSSPP) • Nintendo Switch (Ryujinx/Yuzu) • Wii U (Cemu) • Wii/GameCube (Dolphin) • Xbox 360 (Xenia)
                </TextBlock>
                
                <TextBlock Text="Configure Emulators:" FontWeight="Bold" Margin="0,10,0,5"/>
                <TextBlock TextWrapping="Wrap" FontSize="11" Opacity="0.9">
                    Go to Playnite → Settings → Emulation to add or configure emulators. Once configured, they will appear in the dropdown list above.
                </TextBlock>
                
                <Separator Margin="0,10,0,10"/>
                
                <TextBlock HorizontalAlignment="Right"
                           FontSize="12"
                           Foreground="LimeGreen"
                           FontWeight="Bold"
                           Text="{Binding PluginVersionDisplay, StringFormat=Version: {0}}"/>
                
                <Separator Margin="0,10,0,10"/>
                
                <Button Content="Open Download Manager" 
                        HorizontalAlignment="Stretch"
                        Height="35"
                        Margin="0,5,0,0"
                        Click="OpenDownloadManagerButton_Click"
                        Background="#4CAF50"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
