<Window x:Class="FastInstall.InstallationProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FastInstall - Installation Progress"
        Width="500" Height="320"
        MinWidth="400" MinHeight="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Topmost="False"
        ShowInTaskbar="True"
        Background="#1E1E1E">
    
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style x:Key="ValueStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Game Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Installing:" Style="{StaticResource LabelStyle}"/>
            <TextBlock x:Name="GameNameText" Text="Game Name" Style="{StaticResource TitleStyle}"/>
        </StackPanel>
        
        <!-- Progress Bar -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <ProgressBar x:Name="MainProgressBar" 
                         Height="24" 
                         Minimum="0" 
                         Maximum="100" 
                         Value="0"
                         Background="#333333"
                         Foreground="#4CAF50"/>
            <TextBlock x:Name="ProgressPercentText" 
                       Text="0%" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="White"
                       FontWeight="Bold"
                       FontSize="12"/>
        </Grid>
        
        <!-- Stats Grid -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Left Column -->
            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Progress: " Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="BytesProgressText" Text="0 B / 0 B" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="1" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Speed: " Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="SpeedText" Text="-- MB/s" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="2" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Files: " Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="FilesProgressText" Text="0 / 0" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
            
            <!-- Right Column -->
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Elapsed: " Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="ElapsedText" Text="00:00" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
            
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="Remaining: " Style="{StaticResource LabelStyle}"/>
                <TextBlock x:Name="RemainingText" Text="--:--" Style="{StaticResource ValueStyle}"/>
            </StackPanel>
        </Grid>
        
        <!-- Current File -->
        <StackPanel Grid.Row="3" Margin="0,5,0,10" VerticalAlignment="Top">
            <TextBlock Text="Current file:" Style="{StaticResource LabelStyle}"/>
            <TextBlock x:Name="CurrentFileText" 
                       Text="Preparing..." 
                       Foreground="#AAAAAA"
                       FontSize="11"
                       TextTrimming="CharacterEllipsis"
                       ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}"/>
        </StackPanel>
        
        <!-- Status Message -->
        <TextBlock Grid.Row="4" 
                   x:Name="StatusText" 
                   Text="Copying files..."
                   Foreground="#888888"
                   FontSize="11"
                   Margin="0,0,0,10"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="MinimizeButton" 
                    Content="Minimize" 
                    Width="90" 
                    Height="30" 
                    Margin="0,0,10,0"
                    Click="MinimizeButton_Click"
                    Background="#555555"
                    Foreground="White"
                    BorderThickness="0"/>
            <Button x:Name="PauseButton" 
                    Content="Pause" 
                    Width="90" 
                    Height="30"
                    Margin="0,0,10,0"
                    Click="PauseButton_Click"
                    Background="#FF9800"
                    Foreground="White"
                    BorderThickness="0"
                    Visibility="Collapsed"/>
            <Button x:Name="CancelButton" 
                    Content="Cancel" 
                    Width="90" 
                    Height="30"
                    Click="CancelButton_Click"
                    Background="#D32F2F"
                    Foreground="White"
                    BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
